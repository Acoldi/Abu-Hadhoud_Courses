
## 1. Fibonacci series For Loop
```cpp
#include <iostream>
#include <iomanip>
#include <vector>

using namespace std;

int main(void)
{
	vector <int> vString;
	vString.push_back(1);
	vString.push_back(1);

	for (int i = 2; i < 10; i++)
	{
		vString.push_back(vString[i - 2] + vString[i - 1]);
	}

	for (int iter : vString)
	{
		cout << iter << "  ";
	}



	system("Pause>0");
}
```

### My Sol 2
```cpp
void PrintFiboncciUsingLoop(short Number)
{
	int FibNumber = 1, Prev1 = 0, Prev2 = 0;
	
	for (int i = 1; i < Number; i++)
	{
		cout << Prev1 + FibNumber << "   ";
		Prev2 = Prev1;
		Prev1 = FibNumber;
		FibNumber = Prev1 + Prev2;
	}
}
```

### His Sol
```c++
void PrintFiboncciUsingLoop(short Number)
{
	// This is how we are goning to start!
	int FibNumber = 0, Prev1 = 1, Prev2 = 0;
	// Fib number now is 1
	cout << "1   ";
	for (int i = 1; i < Number; i++)
	{
		FibNumber = Prev1 + Prev2;
		cout << FibNumber << "   ";
		// Advance out prevs, prev 2 equals prev 1
		Prev2 = Prev1;
		// Prev 1 is Fib number.
		Prev1 = FibNumber;
	}
}
```

## 2. Fbonacci Series Using Recursion
- <mark class="hltr-blue">I did not know HOW TO SOLVE IT!!!!!!!!!!!!!</mark>
### His Sol
- Here we use the `Number` as a counter and `Prev1`, `Prev2` are changing according to the incrementation of the `FibNumber` from a base value.
- There is no base case in this recursive function! the initial values are what start the <mark class="hltr-red">Process</mark>.
```c++
void PrintFibonacciUsingRecursion(short Number, int Prev1, int Prev2)
{
	int FibNumber = 0;
	if (Number > 0)
	{
		FibNumber = Prev1 + Prev2;
		cout << FibNumber << "   ";
		Prev2 = Prev1;
		Prev1 = FibNumber;

		PrintFibonacciUsingRecursion(Number - 1, Prev1, Prev2);
	}
}
```
## 3. Print First letters string
### My sol
```c++
void PrintFirstLettersOfString()
{
#include "Basics.h"
#include <iostream>
#include <iomanip>
#include <vector>


using namespace std;

void PrintFirstLettersOfString(string Text)
{
	if (Text[0] != ' ')
	{
		cout << Text[0] << endl;
	}

	for (int i = 0; i < Text.length(); i++)
	{

		if (Text[i] == ' ' && Text[i + 1] != ' ')
		{
			cout << Text[i + 1] << endl;
		}
	}
}

int main(void)
{
	cout << "Please Enter your string:\n ";

	PrintFirstLettersOfString(ReadSting());
	
	system("Pause>0");
}
```

###  Had's sol
```c++
void PrintFirstLettersOfString(string Text)
{
	bool IsFirstLetter = true;

	for (int i = 0; i < Text.length(); i++)
	{
		// If the current letter is not dash and the previous letter is dash
		if (Text[i] != ' ' && IsFirstLetter)
		{
			cout << Text[i] << endl;
		}
		
		IsFirstLetter = (Text[i] == ' ' ? true : false);
	}
}
```

## 4. Upper Case First Letters
```c++
void UpperFirstLetters(string &Text)
{
	bool IsFirstLetter = true;

	for (int i = 0; i < Text.length(); i++)
	{
		// If the current letter is not dash and the previous letter is dash
		if (Text[i] != ' ' && IsFirstLetter)
		{
			Text[i] = Text[i] - 32;
		}
		
		IsFirstLetter = (Text[i] == ' ' ? true : false);
	}
}

int main(void)
{
	cout << "Please Enter your string:\n ";

	string Text = ReadSting();

	UpperFirstLetters(Text);

	cout << Text;
	
	system("Pause>0");
}
```

### His Sol
```c++
void UpperFirstLetters(string &Text)
{
	bool IsFirstLetter = true;

	for (int i = 0; i < Text.length(); i++)
	{
		// If the current letter is not dash and the previous letter is dash
		if (Text[i] != ' ' && IsFirstLetter)
		{
			Text[i] = toupper(Text[i]);
		}
		
		IsFirstLetter = (Text[i] == ' ' ? true : false);
	}
}

int main(void)
{
	cout << "Please Enter your string:\n ";

	string Text = ReadSting();

	UpperFirstLetters(Text);

	cout << Text;
	
	system("Pause>0");
}
```

## . 5 ToLower
```c++
void LowerFirstLetters(string &Text)
{
	bool IsFirstLetter = true;

	for (int i = 0; i < Text.length(); i++)
	{
		// If the current letter is not dash and the previous letter is dash
		if (Text[i] != ' ' && IsFirstLetter)
		{
			Text[i] = Text[i] + 32;
		}
		
		IsFirstLetter = (Text[i] == ' ' ? true : false);
	}
}

int main(void)
{
	cout << "Please Enter your string:\n ";

	string Text = ReadSting();

	LowerFirstLetters(Text);

	cout << Text;
	
	system("Pause>0");
}
```
### His sol 

```c++
void LowerFirstLetters(string &Text)
{
	bool IsFirstLetter = true;

	for (int i = 0; i < Text.length(); i++)
	{
		// If the current letter is not dash and the previous letter is dash
		if (Text[i] != ' ' && IsFirstLetter)
		{
			Text[i] = tolower(Text[i]);
		}
		
		IsFirstLetter = (Text[i] == ' ' ? true : false);
	}
}

int main(void)
{
	cout << "Please Enter your string:\n ";

	string Text = ReadSting();

	LowerFirstLetters(Text);

	cout << Text;
	
	system("Pause>0");
}
```

## 6. upper lower string
```c++
void LowerStringLetters(string &Text)
{

	for (int i = 0; i < Text.length(); i++)
	{
		Text[i] = tolower(Text[i]);
	}
}

void UpperStringLetters(string &Text)
{

	for (int i = 0; i < Text.length(); i++)
	{
		Text[i] = toupper(Text[i]);
	}
}

int main(void)
{
	cout << "Please Enter your string:\n ";

	string Text = ReadSting();

	LowerStringLetters(Text);

	cout << Text << endl << endl;

	UpperStringLetters(Text);
	
	cout << Text;
	system("Pause>0");
}
```

## 7. Invert Letter Case
```c++
void InvertCharacterCase(string &Text)
{

	for (int i = 0; i < Text.length(); i++)
	{
		if (isupper(Text[i]))
			Text[i] = tolower(Text[i]);
		else
			Text[i] = toupper(Text[i]);
	}	
}

```

```cpp
char InvertLetterCase(char C)
{
	return isupper(C) ? tolower(C) : toupper(C);
}


int main(void)
{
	cout << "Please Enter your latter:\n ";

	char CH;
	cin >> CH;

	cout << InvertLetterCase(CH);

	system("Pause>0");
}
```

## 8. Inver Letters Case
```c++
void InvertALLCharactersCase(string& Text)
{
	for (int i = 0; i < Text.length(); i++)
	{
		Text[i] = InvertLetterCase(Text[i]);
	}
}


int main(void)
{
	cout << "Please Enter your text:\n ";

	string text;
	getline(cin,text);

	InvertALLCharactersCase(text);
	
	cout << text;

	system("Pause>0");
}
```


## 9. Count Small Upper letters in a string
```c++
int CountUppercaseLetters(string Text)
{
	int counter = 0;
	for (int i = 0; i < Text.length(); i++)
	{
		if (isupper(Text[i]))
		{
			counter++;
		}
	}
	return counter;
}

int CountLowercaseLetters(string Text)
{
	int counter = 0;
	for (int i = 0; i < Text.length(); i++)
	{
		if (islower(Text[i]))
		{
			counter++;
		}
	}
	return counter;
}


int main(void)
{
	cout << "Please Enter your text:\n ";

	string text = ReadSting();
	cout << text;

	cout << "\nNumber of upper case letters: " << CountUppercaseLetters(text) << endl;
	

	cout << "\nNumber of lower case letters: " << CountLowercaseLetters(text) << endl;

	system("Pause>0");
}
```

```c++
#include "Basics.h"
#include <iostream>
#include <iomanip>
#include <vector>

using namespace std;

enum enCaseLetter { Upper = 1, Lower = 2, All = 3 };


int CountLetterCase(string Text, enCaseLetter Case = enCaseLetter::All)
{
	int counter = 0;
	for (int i = 0; i < Text.length(); i++)
	{
		if (isupper(Text[i]) && Case == enCaseLetter::Upper)
			counter++;

		if (islower(Text[i]) && Case == enCaseLetter::Lower)
			counter++;

		if (Case == enCaseLetter::All)
			return Text.length();

	}
	return counter;
}


int CountUppercaseLetters(string Text)
{
	int counter = 0;
	for (int i = 0; i < Text.length(); i++)
	{
		if (isupper(Text[i]))
		{
			counter++;
		}
	}
	return counter;
}

int CountLowercaseLetters(string Text)
{
	int counter = 0;
	for (int i = 0; i < Text.length(); i++)
	{
		if (islower(Text[i]))
		{
			counter++;
		}
	}
	return counter;
}


int main(void)
{
	cout << "Please Enter your text:\n ";

	string text = ReadSting();
	cout << text;

	cout << "\nText Length = " << CountLetterCase(text);


	cout << "\nNumber of upper case letters: " << CountLetterCase(text, enCaseLetter::Upper) << endl;
	

	cout << "\nNumber of lower case letters: " << CountLetterCase(text, enCaseLetter::Lower) << endl;

	system("Pause>0");
}
```
## 10. Count Letter
```cpp
int CountLetter(string Text, char Letter)
{
	int counter = 0;
	for (int i = 0; i < Text.length(); i++)
	{
		if ((Text[i]) == Letter)
			counter++;
	}
	return counter;
}

int main(void)
{
	cout << "Please Enter your text:\n ";

	string text = ReadSting();
	char Letter;

	cout << "\nEnter the letter you want to count: "; cin >> Letter;
	
	cout << text;

	cout << "\nThe Count Of " << Letter << " = " << CountLetter(text, Letter) << endl; 

	system("Pause>0");
}
```

## 11. Count Letters (Match Case)
```c++
using namespace std;

int CountLetter(string Text, char Letter, bool MatchCase = true)
{
	int counter = 0;

	if (MatchCase)
	{
		for (int i = 0; i < Text.length(); i++)
		{
			if ((Text[i]) == Letter)
				counter++;
		}
		return counter;
	}
	
	for (int i = 0; i < Text.length(); i++)
	{
		if (tolower(Letter) == tolower(Text[i]))
			counter++;
	}
	return counter;
	
}



int main(void)
{
	cout << "Please Enter your text:\n ";

	string text = ReadSting();
	char Letter;

	cout << "\nEnter the letter you want to count: "; cin >> Letter;
	
	cout << text;

	cout << "\nThe Count Of " << Letter << " = " << CountLetter(text, Letter) << endl;

	cout << "\nThe Count Of " << Letter << " case insensetive = " << CountLetter(text, Letter, false) << endl;
	system("Pause>0");
}
```
## 12. Is vowel
```c++
using namespace std;

bool IsVowel(char Letter)
{
	if (tolower(Letter) == 'a' || tolower(Letter) == 'o' || tolower(Letter) == 'i' || tolower(Letter) == 'e' 
		|| tolower(Letter) == 'u')
	{
		return true;
	}
	return false;
}



int main(void)
{
	char Letter;
	
	cout << "\nEnter the letter you want to check: "; cin >> Letter;

	if (IsVowel(Letter))
	{
		cout << "It is a vowel!\n";
	}
	else
		cout << "\nIt is not a vowel!\n";
	
}
```
